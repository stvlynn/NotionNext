# features

Complete user scenarios (e.g. search, comments, dark mode, AI summary). Each feature contains UI, model, API, and logic specific to that scenario.

Currently empty — features will be extracted from `shared/components` as the FSD migration progresses. See [`docs/frontend/layers.md`](../../../docs/frontend/layers.md).
