# entities

Domain data and rules used by the UI. Examples: `Post`, `Category`, `Tag`, `Comment`.

## Contents

- `types/` — shared TypeScript types (migrated from the legacy root `types/`).

## Rules

- No UI, no HTTP clients, no framework-specific code.
- Pure types, factories, validation, and functions operating on the entity.
