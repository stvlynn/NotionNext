# domain

Business rules. No dependencies on frameworks, databases, or transport details.

Currently holds shared domain types to be extracted from `backend/src/infrastructure/db` and `backend/src/application/site` as the DDD migration progresses. See [`docs/backend/domain.md`](../../../docs/backend/domain.md).
